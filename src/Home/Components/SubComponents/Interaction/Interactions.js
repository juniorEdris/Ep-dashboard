const Interactions = (props) => {
    return (
        <div className={`col-12`}>
            <h2 className={`section_header`}>Interactions</h2>

        </div>
     );
}
 
export default Interactions;